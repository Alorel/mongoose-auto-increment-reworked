group: travis_latest
sudo: false
language: node_js
node_js:
  - &latest_tested 9
  - 8
  - 7
  - 6
  - 4

services:
  - mongodb

cache:
  directories:
    - node_modules

after_success: cat ./coverage/lcov.info | coveralls
before_script:
  - npm run build
  - npm run tslint

script: npm test